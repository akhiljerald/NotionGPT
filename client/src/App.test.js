// DEAD FILE — safe to delete.
//
// This was the stock create-react-app placeholder test. It asserted on a
// "learn react" link that this app has never rendered, so it failed whenever it
// ran. The project has no test runner configured after the move to Vite; add
// Vitest (`npm i -D vitest @testing-library/react jsdom`) if you want tests.
